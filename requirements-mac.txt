# ContextPilot – Mac / Apple Silicon (CPU-only) dependencies
# Usage:
#   pip install -r requirements-mac.txt
#   pip install -e . --no-deps

# Core
numpy
scipy
tqdm
aiohttp
ujson

# Retrieval
elasticsearch==8.18.1

# HTTP server
fastapi[all]>=0.115.0
uvicorn[standard]>=0.32.0   # [standard] pulls in uvloop + httptools for better async perf on macOS
httpx>=0.28.0

# Optional: tokenizer for chat-template support in http_server.py
transformers

# Benchmark data preparation (MultihopRAG dataset download)
datasets
